import {React } from 'react';

/**
 * Admin function 
 * 
 * to handle login and verification
 * allows admin to alter the award status of any paper after being authenticated
 * 
 * @author Mehtab Gill
 */

function Profile(){


// return login form and papers with changing visablities depending on if the admin is logged in or not
    return(
        <div className='admin'>
            <h2>profile</h2>
        </div>            
  );
}
export default Profile